import { IconBaseProps } from "react-icons";
import { IoMoon } from "react-icons/io5";


const DarkIcon: React.FC<IconBaseProps> = (props) => {
  return <IoMoon {...props} />;
};

export default DarkIcon;
